Create Table prizes (
id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY,
name VARCHAR (200),
available INT(1)
);

CREATE TABLE phonenumbers (
  id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  number VARCHAR (200),
  available INT(1)
);